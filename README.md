It is needed to run "mvn clean test -P={testType}" command for test launch

There are 2 test types

1. "serviceTests" can be run only if spring project is up and running
2. "mockTests" can be run at any time